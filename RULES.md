# Rules

_Last updated: 2026-06-13_

_Generated by /repo-audit. Manual rules are marked `"source": "manual"`. To suppress a false positive, set `"disabled": true` on the rule — never delete it._

```rules
[
  {
    "id": "no-cross-merchant-query",
    "description": "All D1 queries must include merchant_id filter — no unscoped SELECT across all merchants",
    "pattern": "SELECT.*FROM (customers|transactions|audit_log|checkout_sessions)(?!.*WHERE.*merchant_id)",
    "allowed_in": "",
    "severity": "error",
    "reminder": "Always scope queries with WHERE merchant_id = ?. This enforces tenant isolation per ADR-3.",
    "source": "manual",
    "disabled": false,
    "stale": false
  },
  {
    "id": "no-raw-pii-storage",
    "description": "PII fields (email, profile) must use AES-256-GCM encryption before D1 insert",
    "pattern": "INSERT INTO customers.*email(?!.*_enc)",
    "allowed_in": "",
    "severity": "error",
    "reminder": "Use encrypt() from @g-a-l-a-c-t-i-c/cloudflare before storing PII. Column must be email_enc, not email.",
    "source": "manual",
    "disabled": false,
    "stale": false
  },
  {
    "id": "no-float-money",
    "description": "Money amounts must be stored as INTEGER cents, never REAL/FLOAT",
    "pattern": "REAL|FLOAT|DOUBLE PRECISION",
    "allowed_in": "src/ml/",
    "severity": "error",
    "reminder": "Use INTEGER cents with _cents suffix. Display layer divides by 100. Exception: ML inference output columns in src/ml/.",
    "source": "manual",
    "disabled": false,
    "stale": false
  },
  {
    "id": "no-integer-timestamp",
    "description": "Timestamps must be TEXT ISO-8601, not INTEGER epoch",
    "pattern": "created_at\\s+INTEGER|updated_at\\s+INTEGER",
    "allowed_in": "",
    "severity": "error",
    "reminder": "Use TEXT NOT NULL DEFAULT (datetime('now')) for all timestamps per SQLite/D1 rules.",
    "source": "manual",
    "disabled": false,
    "stale": false
  },
  {
    "id": "no-empty-catch",
    "description": "Silent catch blocks forbidden — every catch must throw, log, or return a typed error",
    "pattern": "catch\\s*\\([^)]*\\)\\s*\\{\\s*\\}",
    "allowed_in": "",
    "severity": "error",
    "reminder": "Throw on every error unless it's intentional null, intentional degraded path, or purely observational async telemetry.",
    "source": "manual",
    "disabled": false,
    "stale": false
  }
]
```

## Rules Summary

| ID | Pattern | Scope | Severity | Source |
|----|---------|-------|----------|--------|
| no-cross-merchant-query | `SELECT.*FROM.*(?!.*merchant_id)` | all | error | manual |
| no-raw-pii-storage | `INSERT INTO customers.*email(?!.*_enc)` | all | error | manual |
| no-float-money | `REAL\|FLOAT\|DOUBLE PRECISION` | except `src/ml/` | error | manual |
| no-integer-timestamp | `created_at INTEGER\|updated_at INTEGER` | all | error | manual |
| no-empty-catch | `catch\s*\([^)]*\)\s*\{\s*\}` | all | error | manual |
